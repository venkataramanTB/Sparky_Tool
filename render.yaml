# Render Infrastructure-as-Code
# Docs: https://render.com/docs/infrastructure-as-code
#
# This file is picked up automatically when you connect this repo to Render.
# It prevents Render from auto-detecting the wrong framework (e.g. Django/gunicorn).

services:
  - type: web
    name: sparky-tool-backend
    # Docker runtime gives a writable filesystem during build so apt-get works.
    # The native Python runtime has a read-only FS that blocks apt-get.
    runtime: docker
    # Dockerfile lives in backend/ but needs the repo root as context so it can
    # reach both frontend/ (Node build) and backend/ (Python app).
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    plan: free

    envVars:
      # ── Database ─────────────────────────────────────────────────────────────
      # PostgreSQL / Neon connection string.
      # Set this in Render dashboard → Environment → Add Environment Variable.
      - key: DATABASE_URL
        sync: false

      # ── Clerk authentication ─────────────────────────────────────────────────
      - key: CLERK_JWKS_URL
        sync: false
      - key: CLERK_API_KEY
        sync: false
      - key: CLERK_API_SECRET
        sync: false

      # ── Encryption ───────────────────────────────────────────────────────────
      # Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
      - key: ENCRYPTION_KEY
        sync: false

      # ── Gemini AI ─────────────────────────────────────────────────────────────
      # Get from https://aistudio.google.com/app/apikey
      - key: GEMINI_API_KEY
        sync: false

      # CORS_ORIGINS is no longer used — CORS is open to all origins.
      # Security is enforced by Clerk JWT validation on every endpoint.
